eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true
    hostname: localhost

logging:
  level:
    com.netflix.eureka: DEBUG
    org.springframework.clo&ud.netflix.eureka: DEBUG